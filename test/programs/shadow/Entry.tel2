module ShadowEntry
import Prelude

type State = Nat

main : Text * State -o Reply State = \x ->
  let (input, state) = x
      _ = input
   in case state of
        0 -> ("", right (shadowValue ()))
        9 -> ("local\n", left ())
        n -> let _ = n in ("wrong\n", left ())
